# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Design reference — the CueFlow UI kit lives on the machine, not in the repo.
# It is the authority for every visual decision here; its rules are written
# into CLAUDE.md so they survive a clone that does not have this folder.
ui-kit
